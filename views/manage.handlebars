<div class="container">
		<div class="panel panel-primary">
			<div class="panel-heading">
				<h3 class="panel-title">Products</h3>
			</div>
			<div class="panel-body">
				{{!-- {{#each products}}
					<input type="text" name="{{this.id}}" value="0">
					<button type="submit" id="{{this.id}}" class="btn btn-success">Add</button>
					<ul>
						<li class="product-list">$ {{this.price}}</li>
						<li class="product-list">{{this.product_name}}</li>
						<li class="product-list">Quantity: {{this.stock_quantity}}</li>
					</ul>
				{{/each}} --}}
			</div>
		</div>
		<div>
		<form>
			<label>Product Name:</label>
			<input type="text" name="productName">
			<label>Current Stock Quantity:</label>
			<input type="text" name="quantity">
			<label>Product Price:</label>
			<input type="text" name="price">
		</form>
		
		</div>
</div>